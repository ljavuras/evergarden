//<%*
tR = `//<%*
tR = "";

const { Templates } = VPS.require("evergarden-templates");
tp = Templates.wrap(tp);

const metadata = app.metadataCache.getFileCache(tp.config.target_file);

// Parsing logic

// Save parsed object to tp
tp.setParsed({
});
//-%\>`;
//-%>