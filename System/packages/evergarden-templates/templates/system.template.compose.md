//<%*
tR = `//<%*
tR = "";

const { Templates } = VPS.require("evergarden-templates");
tp = Templates.wrap(tp);

const parsed = tp.getParsed();
if (!parsed) return;

// Construct note content from \`parsed\` object

// Set note version to new version
tp.setVersion();
//-%\>`;
//-%>